# The repository root is the build context of docker/policy-base/Dockerfile, which copies
# packages/vector-policy and nothing else; the rest of the checkout (the venv above all) stays out
# of the context.
*
!packages/vector-policy
packages/vector-policy/**/__pycache__
packages/vector-policy/**/*.egg-info
packages/vector-policy/**/.pytest_cache
