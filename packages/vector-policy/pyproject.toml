[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "robotensor-vector-policy"
version = "0.2.0"
description = "The policy protocol of Robotensor Vector: a policy served in its own process."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
# Installed into every competitor's image, next to whatever the competitor pins: keep it to these.
dependencies = ["numpy>=1.23", "PyYAML>=6.0"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.hatch.build.targets.wheel]
packages = ["src/vector_policy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"
pythonpath = ["tests"]
markers = [
  "sim: needs a plugged benchmark's simulator (assets, GPU)",
  "container: needs Docker and builds or runs a policy container",
  "slow: takes minutes",
]
